# Рабочий каталог MCP-сервера и общая память

← [Индекс базы знаний](../README.md)

## [правило] Один процесс на все окна — значит, проект не по месту запуска (2026-09-13)

**Контекст:** VibeMemory подключила свой сервер памяти к агенту VibeIDE записью в `~/.vibeide/mcp.json`
и предложила находить установленный сервер самим. По контракту сервер определяет проект стора по
каталогу, в котором его запустили.

**Что оказалось в коде:**
- Клиенты MCP живут в главном процессе одним набором на все окна (`MCPChannel`), и `mcp.json` общий.
- `StdioClientTransport` создавался без `cwd`: процесс наследовал рабочий каталог главного процесса.
- В dev-запуске это репозиторий VibeIDE, у которого проект в сторе есть. Значит, каждая заметка
  агента уходила бы в проект VibeIDE, какую бы папку ни открыли. Живьём не проверено — по коду.

**Как сделано:**
- Поле `cwd` у stdio-сервера в `mcp.json`. Серверам без поля ничего не меняется.
- Найденный сервер `vibememory` запускается из **домашнего** каталога: у него проекта в сторе нет, и
  запись без явного `project` сервер отклоняет, а не кладёт в чужой.
- Своя запись пользователя с именем `vibememory` всегда сильнее найденной.
- Отсутствие сервера — строка в журнале, не уведомление.

**Почему не `cwd` проекта:** два окна с разными проектами делят один процесс — назвать один проект
нечем.

**Открыто:** как агенту узнать имя проекта стора для открытой папки — вопрос отправлен VibeMemory.
До ответа запись без `project` отклоняется.

---

## [грабли] Два поля записи вели себя не так, как обещает тип (2026-09-13)

**Контекст:** при написании спеки `mcp.json` каждое поле сверялось с кодом подключения в
`mcpChannel.ts`, а не с типом.

**Суть:**
- **`headers` не отправляются.** Поле есть в типе `MCPConfigFileEntryJSON`, но
  `StreamableHTTPClientTransport` и `SSEClientTransport` создаются только с адресом — ни в одном из
  трёх путей (HTTP, SSE, откат HTTP→SSE) заголовки не передаются. Сервер с авторизацией заголовком
  так не подключить, и ошибки об этом нет.
- **`env` из файла перекрывается окружением IDE.** Сначала кладутся переменные записи, затем поверх
  них весь `process.env`. Переменную, уже заданную в окружении IDE, из `mcp.json` не переопределить.
  Намеренно это или перепутан порядок — не выяснено; Config Guard при этом ругает перекрытие
  `PATH` и подобных, что намекает на ожидание обратного порядка.

**Исправлено 13.09.2026** (`common/mcpServerEnv.ts`):
- `headers` уходят в `requestInit` обоих транспортов. SSE в SDK 1.29.0 применяет их и к потоку
  событий, и к отправке сообщений через общий `_commonHeaders` — одна опция закрывает все пути.
- Порядок `env` унаследован с первого импорта кода: линт-правка 30.06 его сохранила, обоснования
  нигде нет. Перевёрнут — запись сильнее окружения, как у остальных клиентов MCP.
- **Грабля переворота:** случайный старый порядок был единственным, что не давало чужому конфигу
  подменить `PATH` или подгрузить библиотеку в процесс сервера. Config Guard в режиме по умолчанию
  (`warn`) такое только отмечает. Поэтому имена из `CRITICAL_ENV_OVERRIDES` Config Guard из записи
  не применяются никогда — тот же список, не копия, без учёта регистра (Windows `Path`).

**Применение:** поле формата описывать по коду, который его читает, а не по типу, который его
объявляет. И прежде чем «чинить» неудобный порядок, проверить, не держится ли на нём защита.

